source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'maxmind-db'

# Keeping this outside test group to be able to run tests against image built
# for production
gem 'rack-test'

group :test, :development do
  gem 'pry'
end
